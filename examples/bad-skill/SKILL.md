---
name: Bad_Skill
description: I can help you with stuff. This is a helpful assistant.
allowed-tools: "Read Glob Grep Edit Write Bash"
---

# bad skill

Updated March 2024. If you're doing this before August 2025, use the old method.

PDF (Portable Document Format) is a file format developed by Adobe in 1993.
It is used to present documents in a manner independent of application software,
hardware, and operating systems. PDFs can contain text, images, and more.

HTTP (Hypertext Transfer Protocol) is the foundation of data communication
for the World Wide Web. HTTP functions as a request-response protocol in the
client-server model.

## what to do

Set TIMEOUT to 47 and RETRIES to 5.

You can use pypdf, pdfplumber, PyMuPDF, pdf2image, tika, or camelot.
Each has its own strengths. Try them all and see which works best for you!

You can also use requests, urllib, httpx, or aiohttp for HTTP.
Pick whichever you prefer.

Call the API endpoint to get data. Then use the URL to fetch more info.
The route also accepts POST requests to the endpoint. Check the URL
for the correct route format.

ALWAYS use uppercase variable names.
NEVER use functions longer than 10 lines.
ALWAYS add comments to every line.

## running scripts

Run scripts\helper.py to process the file.
Also check reference\guide.md for more info.

If something goes wrong, just try again.
